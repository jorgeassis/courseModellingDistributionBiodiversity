ecospat.niche.overlap(grid.clim$grid.clim.sp.1, grid.clim$grid.clim.sp.2, cor=F)
grid.clim <- prepareDataNicheOverlap2(environment,speciesA,speciesB)
names(grid.clim)
ecospat.niche.overlap(grid.clim$grid.clim.sp.1, grid.clim$grid.clim.sp.2, cor=F)
eq.test <- ecospat.niche.equivalency.test(grid.clim$grid.clim.sp.1, grid.clim$grid.clim.sp.2,rep=100)
eq.test
sim.test <- ecospat.niche.similarity.test(grid.clim$grid.clim.sp.1, grid.clim$grid.clim.sp.2, rep=100)
eq.test$p.D
eq.test$p.I
eq.test$p.expansion
eq.test$p.D
eq.test$obs
ecospat.niche.overlap(grid.clim$grid.clim.sp.1, grid.clim$grid.clim.sp.2, cor=F)
sim.test$p.D
sim.test$p.I
ecospat.plot.overlap.test(sim.test, "D", "Similarity")
ecospat.plot.overlap.test(sim.test, "I", "Similarity")
t.plot.niche.dyn(grid.clim$grid.clim.sp.1, grid.clim$grid.clim.sp.2)
plot.niche.dyn(grid.clim$grid.clim.sp.1, grid.clim$grid.clim.sp.2)
t.plot.niche.dyn(grid.clim$grid.clim.sp.1, grid.clim$grid.clim.sp.2)
# Plot a histogram of observed and randomly simulated overlaps, with p-values of equivalency or similarity tests.
t.plot.niche.dyn(grid.clim$grid.clim.sp.1, grid.clim$grid.clim.sp.2)
ecospat.plot.overlap.test(sim.test, "D", "Similarity")
ecospat.plot.overlap.test(sim.test, "I", "Similarity")
ecospat.plot.niche.dyn(grid.clim$grid.clim.sp.1, grid.clim$grid.clim.sp.2)
niche.size
?niche.size
spec
library(terra)
library(ecospat)
library(dismo)
library(nicheROVER)
library(ade4)
# load environmental predictors
environment <- rast(list.files(path = "../Data/Raster data", pattern = "Present Surface", full.names = TRUE))
# load binary SDM outputs
speciesA <- rast("../Data/Raster data/Laminaria hyperborea Suitability.tif")
speciesB <- rast("../Data/Raster data/Laminaria ochroleuca Suitability.tif")
names(speciesA) <- "Laminaria hyperborea"
names(speciesB) <- "Laminaria ochroleuca"
data <- prepareDataNicheOverlap(environment,speciesA,speciesB)
repetitions <- 1000
colorsGroups <- c("grey", "red")
plotNicheOverlap(data,repetitions)
meanNicheOverlap(data,repetitions,colorsGroups)
par(mfrow = c(1,1))
overlap
?overlap
overlap(data)
over.stat <- overlap(data, nreps = 10, nprob = 1e3, alpha = c(.95, 0.99))
nsamples=1000
species.par <- tapply(1:nrow(data), data$species, function(ii) niw.post(nsamples = nsamples, X = data[ii, 2:ncol(data)]))
clrs <- c("black", "red")  # colors for each species
species.par <- tapply(1:nrow(data), data$species, function(ii) niw.post(nsamples = nsamples, X = data[ii, 2:ncol(data)]))
species.par.data <- tapply(1:nrow(data), data$species, function(ii) X = data[ii, 2:ncol(data)])
niche.plot(niche.par = species.par, ndens=1000, niche.data = species.par.data, pfrac = 0, col = clrs, xlab = expression("Niche overlap"))
over.stat <- overlap(species.par, nreps = nsamples, nprob = 1e3, alpha = c(.95, 0.99))
over.stat
fish.size <- sapply(species.par, function(spec) {apply(spec$Sigma, 3, niche.size, alpha = .95)
fish.size <- sapply(species.par, function(spec) {apply(spec$Sigma, 3, niche.size, alpha = .95)})
fish.size
niche.size <- sapply(species.par, function(spec) {apply(spec$Sigma, 3, niche.size, alpha = .95)})
# point estimate and standard error
rbind(est = colMeans(fish.size),
se = apply(fish.size, 2, sd))
nicheSizeOverlap <- function(data,nsamples) {
species.par <- tapply(1:nrow(data), data$species, function(ii) niw.post(nsamples = nsamples, X = data[ii, 2:ncol(data)]))
clrs <- c("black", "red")  # colors for each species
nsamples <- 10
species.par <- tapply(1:nrow(data), data$species, function(ii) niw.post(nsamples = nsamples, X = data[ii, 2:ncol(data)]))
species.par.data <- tapply(1:nrow(data), data$species, function(ii) X = data[ii, 2:ncol(data)])
niche.plot(niche.par = species.par, ndens=1000, niche.data = species.par.data, pfrac = 0, col = clrs, xlab = expression("Niche overlap"))
niche.size <- sapply(species.par, function(spec) {apply(spec$Sigma, 3, niche.size, alpha = .95)})
# point estimate and standard error
rbind(est = colMeans(fish.size), se = apply(fish.size, 2, sd))
}
nicheSize <- function(data,nsamples) {
species.par <- tapply(1:nrow(data), data$species, function(ii) niw.post(nsamples = nsamples, X = data[ii, 2:ncol(data)]))
clrs <- c("black", "red")  # colors for each species
species.par <- tapply(1:nrow(data), data$species, function(ii) niw.post(nsamples = nsamples, X = data[ii, 2:ncol(data)]))
species.par.data <- tapply(1:nrow(data), data$species, function(ii) X = data[ii, 2:ncol(data)])
niche.plot(niche.par = species.par, ndens=1000, niche.data = species.par.data, pfrac = 0, col = clrs, xlab = expression("Niche overlap"))
niche.size <- sapply(species.par, function(spec) {apply(spec$Sigma, 3, niche.size, alpha = .95)})
# point estimate and standard error
rbind(est = colMeans(fish.size), se = apply(fish.size, 2, sd))
}
meanNicheOverlap <- function(data,nsamples,colors.sp) {
species.par <- tapply(1:nrow(data), data$species, function(ii) niw.post(nsamples = nsamples, X = data[ii, 2:ncol(data)]))
# Overlap calculation.  use nsamples = nprob = 10000 (1e4) for higher
# accuracy.  the variable over.stat can be supplied directly to the
# overlap.plot function
over.stat <- nicheROVER::overlap(species.par, nreps = nsamples, nprob = 10000, alpha = c(0.95, 0.99))
cat( paste0("\n"))
cat( paste0("............................................................."))
cat( paste0("\n"))
cat( paste0("\n"))
cat( paste0("The mean overlap metrics calculated across iteratations for both niche region sizes"))
cat( paste0("\n"))
over.mean <- apply(over.stat, c(1:2, 4), mean) * 100
print(round(over.mean[, ,  1], 2))
cat( paste0("\n"))
cat( paste0("............................................................."))
cat( paste0("\n"))
cat( paste0("\n"))
cat( paste0("Credible interval"))
cat( paste0("\n"))
over.cred <- apply(over.stat * 100, c(1:2, 4), quantile, prob = c(0.025, 0.975), na.rm = TRUE)
print(round(over.cred[, , , 1]),2)  # display alpha = .95 niche region
# Overlap plot.Before you run this, make sure that you have chosen your
# alpha ecoregionsLevel.
clrs <- colors.sp  # colors for each species
over.stat <- nicheROVER::overlap(species.par, nreps = nsamples, nprob = 1000, alpha = 0.95)
overlap.plot(over.stat, col = clrs, mean.cred.col = "turquoise", equal.axis = TRUE, xlab = "Overlap Probability (%) -- Niche Region Size: 95%")
}
plotNicheOverlap <- function(data,nsamples) {
species.par <- tapply(1:nrow(data), data$species, function(ii) niw.post(nsamples = nsamples, X = data[ii, 2:ncol(data)]))
clrs <- c("black", "red")  # colors for each species
species.par <- tapply(1:nrow(data), data$species, function(ii) niw.post(nsamples = nsamples, X = data[ii, 2:ncol(data)]))
species.par.data <- tapply(1:nrow(data), data$species, function(ii) X = data[ii, 2:ncol(data)])
niche.plot(niche.par = species.par, ndens=1000, niche.data = species.par.data, pfrac = 0, col = clrs, xlab = expression("Niche overlap"))
over.stat <- overlap(species.par, nreps = nsamples, nprob = 1e3, alpha = c(.95, 0.99))
niche.size <- sapply(species.par, function(spec) {apply(spec$Sigma, 3, niche.size, alpha = .95)})
# point estimate and standard error
rbind(est = colMeans(fish.size), se = apply(fish.size, 2, sd))
}
nicheSize <- function(data,nsamples) {
species.par <- tapply(1:nrow(data), data$species, function(ii) niw.post(nsamples = nsamples, X = data[ii, 2:ncol(data)]))
clrs <- c("black", "red")  # colors for each species
species.par <- tapply(1:nrow(data), data$species, function(ii) niw.post(nsamples = nsamples, X = data[ii, 2:ncol(data)]))
species.par.data <- tapply(1:nrow(data), data$species, function(ii) X = data[ii, 2:ncol(data)])
niche.plot(niche.par = species.par, ndens=1000, niche.data = species.par.data, pfrac = 0, col = clrs, xlab = expression("Niche overlap"))
niche.size <- sapply(species.par, function(spec) {apply(spec$Sigma, 3, niche.size, alpha = .95)})
# point estimate and standard error
rbind(est = colMeans(fish.size), se = apply(fish.size, 2, sd))
}
prepareDataNicheOverlap <- function(environment,rasterSPA,rasterSPB) {
occurrenceRecordsSpA <- as.data.frame(rasterSPA, xy=T)
occurrenceRecordsSpA <- occurrenceRecordsSpA[occurrenceRecordsSpA[,names(rasterSPA)] == 1,c("x","y")]
occurrenceRecordsSpB <- as.data.frame(rasterSPB, xy=T)
occurrenceRecordsSpB <- occurrenceRecordsSpB[occurrenceRecordsSpB[,names(rasterSPB)] == 1,c("x","y")]
environment.used.by.species.1 <- terra::extract(environment,occurrenceRecordsSpA,ID=F)
environment.used.by.species.1 <- environment.used.by.species.1[complete.cases(environment.used.by.species.1),]
environment.used.by.species.2 <- terra::extract(environment,occurrenceRecordsSpB,ID=F)
environment.used.by.species.2 <- environment.used.by.species.2[complete.cases(environment.used.by.species.2),]
data <- data.frame(  species = c(rep(names(rasterSPA),nrow(environment.used.by.species.1)),rep(names(rasterSPB),nrow(environment.used.by.species.2))) ,
rbind(environment.used.by.species.1,environment.used.by.species.2) )
print(aggregate(data[2:ncol(data)], data[1], mean))
return(data)
}
prepareDataNicheOverlap2 <- function(environment,rasterSPA,rasterSPB) {
names(rasterSPA) <- "A"
names(rasterSPB) <- "B"
occurrenceRecordsSpA <- as.data.frame(rasterSPA, xy=T)
occurrenceRecordsSpA <- occurrenceRecordsSpA[occurrenceRecordsSpA$A == 1,c("x","y")]
occurrenceRecordsSpB <- as.data.frame(rasterSPB, xy=T)
occurrenceRecordsSpB <- occurrenceRecordsSpB[occurrenceRecordsSpB$B == 1,c("x","y")]
environment.used.by.species.1 <- terra::extract(environment,occurrenceRecordsSpA,ID=F)
environment.used.by.species.1 <- environment.used.by.species.1[complete.cases(environment.used.by.species.1),]
environment.used.by.species.2 <- terra::extract(environment,occurrenceRecordsSpB,ID=F)
environment.used.by.species.2 <- environment.used.by.species.2[complete.cases(environment.used.by.species.2),]
environment.used.by.species.1 <- environment.used.by.species.1[complete.cases(environment.used.by.species.1),]
environment.used.by.species.2 <- environment.used.by.species.2[complete.cases(environment.used.by.species.2),]
environment.used.by.species.1 <- environment.used.by.species.1[sample(1:nrow(environment.used.by.species.1), min(c(nrow(environment.used.by.species.1),1000)), replace = F), ]
environment.used.by.species.2 <- environment.used.by.species.2[sample(1:nrow(environment.used.by.species.2), min(c(nrow(environment.used.by.species.2),1000)), replace = F), ]
env.used <- rbind(environment.used.by.species.1,environment.used.by.species.2)
# env.used <- rbind(rnd.points.sp.1.env,rnd.points.sp.2.env)
pca.env.used <- dudi.pca(env.used,scannf=F,nf=2)
ecospat.plot.contrib(contrib=pca.env.used$co, eigen=pca.env.used$eig)
sp.vect <- c( rep("env.sp1",nrow(environment.used.by.species.1)) ,
rep("env.sp2",nrow(environment.used.by.species.2)) ,
rep("sp1",nrow(environment.used.by.species.1)) ,
rep("sp2",nrow(environment.used.by.species.2)) )
# PCA scores for the whole study area
scores.globclim <- pca.env.used$li
# PCA scores for the species native distribution
scores.sp.1 <- suprow(pca.env.used,environment.used.by.species.1)$li
scores.sp.2 <- suprow(pca.env.used,environment.used.by.species.2)$li
scores.clim.sp.1 <- suprow(pca.env.used,environment.used.by.species.1)$li
scores.clim.sp.2 <- suprow(pca.env.used,environment.used.by.species.2)$li
grid.clim.sp.1 <- ecospat.grid.clim.dyn(glob=scores.globclim, glob1=scores.clim.sp.1,
sp=scores.sp.1, R=100,
th.sp=0)
grid.clim.sp.2 <- ecospat.grid.clim.dyn(glob=scores.globclim, glob1=scores.clim.sp.2,
sp=scores.sp.2, R=100,
th.sp=0)
grid.clim <- list("grid.clim.sp.1" = grid.clim.sp.1, "grid.clim.sp.2" = grid.clim.sp.2)
return(grid.clim)
}
repetitions <- 100
plotNicheOverlap(data,repetitions)
nicheSize(data,repetitions)
meanNicheOverlap <- function(data,nsamples,colors.sp) {
species.par <- tapply(1:nrow(data), data$species, function(ii) niw.post(nsamples = nsamples, X = data[ii, 2:ncol(data)]))
# Overlap calculation.  use nsamples = nprob = 10000 (1e4) for higher
# accuracy.  the variable over.stat can be supplied directly to the
# overlap.plot function
over.stat <- nicheROVER::overlap(species.par, nreps = nsamples, nprob = 10000, alpha = c(0.95, 0.99))
cat( paste0("\n"))
cat( paste0("............................................................."))
cat( paste0("\n"))
cat( paste0("\n"))
cat( paste0("The mean overlap metrics calculated across iteratations for both niche region sizes"))
cat( paste0("\n"))
over.mean <- apply(over.stat, c(1:2, 4), mean) * 100
print(round(over.mean[, ,  1], 2))
cat( paste0("\n"))
cat( paste0("............................................................."))
cat( paste0("\n"))
cat( paste0("\n"))
cat( paste0("Credible interval"))
cat( paste0("\n"))
over.cred <- apply(over.stat * 100, c(1:2, 4), quantile, prob = c(0.025, 0.975), na.rm = TRUE)
print(round(over.cred[, , , 1]),2)  # display alpha = .95 niche region
# Overlap plot.Before you run this, make sure that you have chosen your
# alpha ecoregionsLevel.
clrs <- colors.sp  # colors for each species
over.stat <- nicheROVER::overlap(species.par, nreps = nsamples, nprob = 1000, alpha = 0.95)
overlap.plot(over.stat, col = clrs, mean.cred.col = "turquoise", equal.axis = TRUE, xlab = "Overlap Probability (%) -- Niche Region Size: 95%")
}
plotNicheOverlap <- function(data,nsamples) {
species.par <- tapply(1:nrow(data), data$species, function(ii) niw.post(nsamples = nsamples, X = data[ii, 2:ncol(data)]))
clrs <- c("black", "red")  # colors for each species
nsamples <- 10
species.par <- tapply(1:nrow(data), data$species, function(ii) niw.post(nsamples = nsamples, X = data[ii, 2:ncol(data)]))
species.par.data <- tapply(1:nrow(data), data$species, function(ii) X = data[ii, 2:ncol(data)])
niche.plot(niche.par = species.par, ndens=1000, niche.data = species.par.data, pfrac = 0, col = clrs, xlab = expression("Niche overlap"))
}
nicheSize <- function(data,nsamples) {
species.par <- tapply(1:nrow(data), data$species, function(ii) niw.post(nsamples = nsamples, X = data[ii, 2:ncol(data)]))
clrs <- c("black", "red")  # colors for each species
species.par <- tapply(1:nrow(data), data$species, function(ii) niw.post(nsamples = nsamples, X = data[ii, 2:ncol(data)]))
species.par.data <- tapply(1:nrow(data), data$species, function(ii) X = data[ii, 2:ncol(data)])
niche.plot(niche.par = species.par, ndens=1000, niche.data = species.par.data, pfrac = 0, col = clrs, xlab = expression("Niche overlap"))
niche.size <- sapply(species.par, function(spec) {apply(spec$Sigma, 3, niche.size, alpha = .95)})
# point estimate and standard error
rbind(est = colMeans(fish.size), se = apply(fish.size, 2, sd))
}
prepareDataNicheOverlap <- function(environment,rasterSPA,rasterSPB) {
occurrenceRecordsSpA <- as.data.frame(rasterSPA, xy=T)
occurrenceRecordsSpA <- occurrenceRecordsSpA[occurrenceRecordsSpA[,names(rasterSPA)] == 1,c("x","y")]
occurrenceRecordsSpB <- as.data.frame(rasterSPB, xy=T)
occurrenceRecordsSpB <- occurrenceRecordsSpB[occurrenceRecordsSpB[,names(rasterSPB)] == 1,c("x","y")]
environment.used.by.species.1 <- terra::extract(environment,occurrenceRecordsSpA,ID=F)
environment.used.by.species.1 <- environment.used.by.species.1[complete.cases(environment.used.by.species.1),]
environment.used.by.species.2 <- terra::extract(environment,occurrenceRecordsSpB,ID=F)
environment.used.by.species.2 <- environment.used.by.species.2[complete.cases(environment.used.by.species.2),]
data <- data.frame(  species = c(rep(names(rasterSPA),nrow(environment.used.by.species.1)),rep(names(rasterSPB),nrow(environment.used.by.species.2))) ,
rbind(environment.used.by.species.1,environment.used.by.species.2) )
print(aggregate(data[2:ncol(data)], data[1], mean))
return(data)
}
prepareDataNicheOverlap2 <- function(environment,rasterSPA,rasterSPB) {
names(rasterSPA) <- "A"
names(rasterSPB) <- "B"
occurrenceRecordsSpA <- as.data.frame(rasterSPA, xy=T)
occurrenceRecordsSpA <- occurrenceRecordsSpA[occurrenceRecordsSpA$A == 1,c("x","y")]
occurrenceRecordsSpB <- as.data.frame(rasterSPB, xy=T)
occurrenceRecordsSpB <- occurrenceRecordsSpB[occurrenceRecordsSpB$B == 1,c("x","y")]
environment.used.by.species.1 <- terra::extract(environment,occurrenceRecordsSpA,ID=F)
environment.used.by.species.1 <- environment.used.by.species.1[complete.cases(environment.used.by.species.1),]
environment.used.by.species.2 <- terra::extract(environment,occurrenceRecordsSpB,ID=F)
environment.used.by.species.2 <- environment.used.by.species.2[complete.cases(environment.used.by.species.2),]
environment.used.by.species.1 <- environment.used.by.species.1[complete.cases(environment.used.by.species.1),]
environment.used.by.species.2 <- environment.used.by.species.2[complete.cases(environment.used.by.species.2),]
environment.used.by.species.1 <- environment.used.by.species.1[sample(1:nrow(environment.used.by.species.1), min(c(nrow(environment.used.by.species.1),1000)), replace = F), ]
environment.used.by.species.2 <- environment.used.by.species.2[sample(1:nrow(environment.used.by.species.2), min(c(nrow(environment.used.by.species.2),1000)), replace = F), ]
env.used <- rbind(environment.used.by.species.1,environment.used.by.species.2)
# env.used <- rbind(rnd.points.sp.1.env,rnd.points.sp.2.env)
pca.env.used <- dudi.pca(env.used,scannf=F,nf=2)
ecospat.plot.contrib(contrib=pca.env.used$co, eigen=pca.env.used$eig)
sp.vect <- c( rep("env.sp1",nrow(environment.used.by.species.1)) ,
rep("env.sp2",nrow(environment.used.by.species.2)) ,
rep("sp1",nrow(environment.used.by.species.1)) ,
rep("sp2",nrow(environment.used.by.species.2)) )
# PCA scores for the whole study area
scores.globclim <- pca.env.used$li
# PCA scores for the species native distribution
scores.sp.1 <- suprow(pca.env.used,environment.used.by.species.1)$li
scores.sp.2 <- suprow(pca.env.used,environment.used.by.species.2)$li
scores.clim.sp.1 <- suprow(pca.env.used,environment.used.by.species.1)$li
scores.clim.sp.2 <- suprow(pca.env.used,environment.used.by.species.2)$li
grid.clim.sp.1 <- ecospat.grid.clim.dyn(glob=scores.globclim, glob1=scores.clim.sp.1,
sp=scores.sp.1, R=100,
th.sp=0)
grid.clim.sp.2 <- ecospat.grid.clim.dyn(glob=scores.globclim, glob1=scores.clim.sp.2,
sp=scores.sp.2, R=100,
th.sp=0)
grid.clim <- list("grid.clim.sp.1" = grid.clim.sp.1, "grid.clim.sp.2" = grid.clim.sp.2)
return(grid.clim)
}
plotNicheOverlap(data,repetitions)
nicheSize(data,repetitions)
meanNicheOverlap(data,repetitions,colorsGroups)
niche.par.plot(species.par, col = colorsGroups, plot.index = 1)
niche.par.plot(species.par, col = colorsGroups, plot.index = 2)
niche.par.plot(species.par, col = colorsGroups, plot.index = 3)
niche.par.plot(species.par, col = colorsGroups, plot.index = 4)
dev.off()
par(mar = c(4, 4, .5, .1)+.1, mfrow = c(1,3))
niche.par.plot(species.par, col = clrs, plot.index = 1)
niche.par.plot(species.par, col = clrs, plot.index = 2)
niche.par.plot(species.par, col = clrs, plot.index = 1:2)
legend("topleft", legend = names(species.par), fill = clrs)
plotNicheOverlap(data,repetitions)
?niche.plot
plotNicheOverlap(data,repetitions)
# load environmental predictors
environment <- rast(list.files(path = "../Data/Raster data", pattern = "Present Surface", full.names = TRUE))
names(environment)
names(environment) <- c("Nitrate","Temperature Max.","Temperature Min")
# load binary SDM outputs
speciesA <- rast("../Data/Raster data/Laminaria hyperborea Suitability.tif")
speciesB <- rast("../Data/Raster data/Laminaria ochroleuca Suitability.tif")
names(speciesA) <- "Laminaria hyperborea"
names(speciesB) <- "Laminaria ochroleuca"
data <- prepareDataNicheOverlap(environment,speciesA,speciesB)
repetitions <- 100
colorsGroups <- c("grey", "red")
plotNicheOverlap(data,repetitions)
niche.plot(niche.par = species.par)
niche.plot(niche.par = species.par,niche.data = species.par.data)
niche.plot(niche.par = species.par,niche.data = species.par.data, col = clrs)
a <- niche.plot(niche.par = species.par,niche.data = species.par.data, col = clrs)
names(a)
a
a$mar
a$mfcol
a$oma
a[[1]]
a[1]
niche.plot
library(terra)
library(ecospat)
library(dismo)
library(nicheROVER)
library(terra)
library(ecospat)
library(dismo)
library(nicheROVER)
library(ade4)
source("Session-P-S09-functions.R")
getwd()
getwd("../")
list.files("../")
list.files("../Sessions practice")
library(terra)
library(ecospat)
library(dismo)
library(nicheROVER)
library(ade4)
source("../Sessions practice/Session-P-S09-functions.R")
# load environmental predictors
environment <- rast(list.files(path = "../Data/Raster data", pattern = "Present Surface", full.names = TRUE))
names(environment) <- c("Nitrate","Temperature Max.","Temperature Min")
plot(environment)
# load binary SDM outputs
speciesA <- rast("../Data/Raster data/Laminaria hyperborea Suitability.tif")
speciesB <- rast("../Data/Raster data/Laminaria ochroleuca Suitability.tif")
names(speciesA) <- "Laminaria hyperborea"
names(speciesB) <- "Laminaria ochroleuca"
plot(speciesA)
dev.off()
library(terra)
library(ecospat)
library(dismo)
library(nicheROVER)
library(ade4)
source("../Sessions practice/Session-P-S09-functions.R")
# load environmental predictors
environment <- rast(list.files(path = "../Data/Raster data", pattern = "Present Surface", full.names = TRUE))
names(environment) <- c("Nitrate","Temperature Max.","Temperature Min")
plot(environment)
# load binary SDM outputs
speciesA <- rast("../Data/Raster data/Laminaria hyperborea Suitability.tif")
speciesB <- rast("../Data/Raster data/Laminaria ochroleuca Suitability.tif")
names(speciesA) <- "Laminaria hyperborea"
names(speciesB) <- "Laminaria ochroleuca"
plot(speciesA)
plot(speciesA, col=c("Black","Yellow"))
plot(speciesA, col=c("Yellow","Black"))
plot(speciesB, col=c("Yellow","Black"))
data <- prepareDataNicheOverlap(environment,speciesA,speciesB)
repetitions <- 100
colorsGroups <- c("grey", "red")
plotNicheOverlap(data,repetitions)
nicheSize(data,repetitions)
meanNicheOverlap <- function(data,nsamples,colors.sp) {
species.par <- tapply(1:nrow(data), data$species, function(ii) niw.post(nsamples = nsamples, X = data[ii, 2:ncol(data)]))
# Overlap calculation.  use nsamples = nprob = 10000 (1e4) for higher
# accuracy.  the variable over.stat can be supplied directly to the
# overlap.plot function
over.stat <- nicheROVER::overlap(species.par, nreps = nsamples, nprob = 10000, alpha = c(0.95, 0.99))
cat( paste0("\n"))
cat( paste0("............................................................."))
cat( paste0("\n"))
cat( paste0("\n"))
cat( paste0("The mean overlap metrics calculated across iteratations for both niche region sizes"))
cat( paste0("\n"))
over.mean <- apply(over.stat, c(1:2, 4), mean) * 100
print(round(over.mean[, ,  1], 2))
cat( paste0("\n"))
cat( paste0("............................................................."))
cat( paste0("\n"))
cat( paste0("\n"))
cat( paste0("Credible interval"))
cat( paste0("\n"))
over.cred <- apply(over.stat * 100, c(1:2, 4), quantile, prob = c(0.025, 0.975), na.rm = TRUE)
print(round(over.cred[, , , 1]),2)  # display alpha = .95 niche region
# Overlap plot.Before you run this, make sure that you have chosen your
# alpha ecoregionsLevel.
clrs <- colors.sp  # colors for each species
over.stat <- nicheROVER::overlap(species.par, nreps = nsamples, nprob = 1000, alpha = 0.95)
overlap.plot(over.stat, col = clrs, mean.cred.col = "turquoise", equal.axis = TRUE, xlab = "Overlap Probability (%) -- Niche Region Size: 95%")
}
plotNicheOverlap <- function(data,nsamples) {
species.par <- tapply(1:nrow(data), data$species, function(ii) niw.post(nsamples = nsamples, X = data[ii, 2:ncol(data)]))
clrs <- c("black", "red")  # colors for each species
nsamples <- 10
species.par <- tapply(1:nrow(data), data$species, function(ii) niw.post(nsamples = nsamples, X = data[ii, 2:ncol(data)]))
species.par.data <- tapply(1:nrow(data), data$species, function(ii) X = data[ii, 2:ncol(data)])
niche.plot(niche.par = species.par, ndens=1000, niche.data = species.par.data, pfrac = 0, col = clrs, xlab = expression("Niche overlap"))
}
nicheSize <- function(data,nsamples) {
species.par <- tapply(1:nrow(data), data$species, function(ii) niw.post(nsamples = nsamples, X = data[ii, 2:ncol(data)]))
clrs <- c("black", "red")  # colors for each species
species.par <- tapply(1:nrow(data), data$species, function(ii) niw.post(nsamples = nsamples, X = data[ii, 2:ncol(data)]))
species.par.data <- tapply(1:nrow(data), data$species, function(ii) X = data[ii, 2:ncol(data)])
niche.plot(niche.par = species.par, ndens=1000, niche.data = species.par.data, pfrac = 0, col = clrs, xlab = expression("Niche overlap"))
niche.size <- sapply(species.par, function(spec) {apply(spec$Sigma, 3, niche.size, alpha = .95)})
# point estimate and standard error
rbind(est = colMeans(niche.size), se = apply(niche.size, 2, sd))
}
prepareDataNicheOverlap <- function(environment,rasterSPA,rasterSPB) {
occurrenceRecordsSpA <- as.data.frame(rasterSPA, xy=T)
occurrenceRecordsSpA <- occurrenceRecordsSpA[occurrenceRecordsSpA[,names(rasterSPA)] == 1,c("x","y")]
occurrenceRecordsSpB <- as.data.frame(rasterSPB, xy=T)
occurrenceRecordsSpB <- occurrenceRecordsSpB[occurrenceRecordsSpB[,names(rasterSPB)] == 1,c("x","y")]
environment.used.by.species.1 <- terra::extract(environment,occurrenceRecordsSpA,ID=F)
environment.used.by.species.1 <- environment.used.by.species.1[complete.cases(environment.used.by.species.1),]
environment.used.by.species.2 <- terra::extract(environment,occurrenceRecordsSpB,ID=F)
environment.used.by.species.2 <- environment.used.by.species.2[complete.cases(environment.used.by.species.2),]
data <- data.frame(  species = c(rep(names(rasterSPA),nrow(environment.used.by.species.1)),rep(names(rasterSPB),nrow(environment.used.by.species.2))) ,
rbind(environment.used.by.species.1,environment.used.by.species.2) )
print(aggregate(data[2:ncol(data)], data[1], mean))
return(data)
}
prepareDataNicheOverlap2 <- function(environment,rasterSPA,rasterSPB) {
names(rasterSPA) <- "A"
names(rasterSPB) <- "B"
occurrenceRecordsSpA <- as.data.frame(rasterSPA, xy=T)
occurrenceRecordsSpA <- occurrenceRecordsSpA[occurrenceRecordsSpA$A == 1,c("x","y")]
occurrenceRecordsSpB <- as.data.frame(rasterSPB, xy=T)
occurrenceRecordsSpB <- occurrenceRecordsSpB[occurrenceRecordsSpB$B == 1,c("x","y")]
environment.used.by.species.1 <- terra::extract(environment,occurrenceRecordsSpA,ID=F)
environment.used.by.species.1 <- environment.used.by.species.1[complete.cases(environment.used.by.species.1),]
environment.used.by.species.2 <- terra::extract(environment,occurrenceRecordsSpB,ID=F)
environment.used.by.species.2 <- environment.used.by.species.2[complete.cases(environment.used.by.species.2),]
environment.used.by.species.1 <- environment.used.by.species.1[complete.cases(environment.used.by.species.1),]
environment.used.by.species.2 <- environment.used.by.species.2[complete.cases(environment.used.by.species.2),]
environment.used.by.species.1 <- environment.used.by.species.1[sample(1:nrow(environment.used.by.species.1), min(c(nrow(environment.used.by.species.1),1000)), replace = F), ]
environment.used.by.species.2 <- environment.used.by.species.2[sample(1:nrow(environment.used.by.species.2), min(c(nrow(environment.used.by.species.2),1000)), replace = F), ]
env.used <- rbind(environment.used.by.species.1,environment.used.by.species.2)
# env.used <- rbind(rnd.points.sp.1.env,rnd.points.sp.2.env)
pca.env.used <- dudi.pca(env.used,scannf=F,nf=2)
ecospat.plot.contrib(contrib=pca.env.used$co, eigen=pca.env.used$eig)
sp.vect <- c( rep("env.sp1",nrow(environment.used.by.species.1)) ,
rep("env.sp2",nrow(environment.used.by.species.2)) ,
rep("sp1",nrow(environment.used.by.species.1)) ,
rep("sp2",nrow(environment.used.by.species.2)) )
# PCA scores for the whole study area
scores.globclim <- pca.env.used$li
# PCA scores for the species native distribution
scores.sp.1 <- suprow(pca.env.used,environment.used.by.species.1)$li
scores.sp.2 <- suprow(pca.env.used,environment.used.by.species.2)$li
scores.clim.sp.1 <- suprow(pca.env.used,environment.used.by.species.1)$li
scores.clim.sp.2 <- suprow(pca.env.used,environment.used.by.species.2)$li
grid.clim.sp.1 <- ecospat.grid.clim.dyn(glob=scores.globclim, glob1=scores.clim.sp.1,
sp=scores.sp.1, R=100,
th.sp=0)
grid.clim.sp.2 <- ecospat.grid.clim.dyn(glob=scores.globclim, glob1=scores.clim.sp.2,
sp=scores.sp.2, R=100,
th.sp=0)
grid.clim <- list("grid.clim.sp.1" = grid.clim.sp.1, "grid.clim.sp.2" = grid.clim.sp.2)
return(grid.clim)
}
repetitions <- 100
colorsGroups <- c("grey", "red")
plotNicheOverlap(data,repetitions)
nicheSize(data,repetitions)
meanNicheOverlap(data,repetitions,colorsGroups)
nicheSize(data,repetitions)
meanNicheOverlap(data,repetitions,colorsGroups)
grid.clim <- prepareDataNicheOverlap2(environment,speciesA,speciesB)
dev,off()
dev.off()
grid.clim <- prepareDataNicheOverlap2(environment,speciesA,speciesB)
ecospat.niche.overlap(grid.clim$grid.clim.sp.1, grid.clim$grid.clim.sp.2, cor=F)
eq.test <- ecospat.niche.equivalency.test(grid.clim$grid.clim.sp.1, grid.clim$grid.clim.sp.2,rep=1000)
eq.test <- ecospat.niche.equivalency.test(grid.clim$grid.clim.sp.1, grid.clim$grid.clim.sp.2,rep=100)
eq.test$p.D
eq.test$p.I
sim.test <- ecospat.niche.similarity.test(grid.clim$grid.clim.sp.1, grid.clim$grid.clim.sp.2, rep=100)
sim.test$p.D
sim.test$p.I
ecospat.plot.niche.dyn(grid.clim$grid.clim.sp.1, grid.clim$grid.clim.sp.2)
niche.dyn <- ecospat.niche.dyn.index(grid.clim$grid.clim.sp.1, grid.clim$grid.clim.sp.2, intersection=0.1)
niche.dyn$dyn
niche.dyn$dynamic.index.w
niche.dyn$category_quantity
plot(niche.dyn <- ecospat.niche.dyn.index(grid.clim$grid.clim.sp.1, grid.clim$grid.clim.sp.2, intersection=0.1)
niche.dyn$dyn)
plot(niche.dyn$dyn)
